# 数据库存储目录（默认为 ./data）
DB_DIR=./data

# 服务端口（默认 3000）
PORT=3000

# 以下配置已迁移到数据库，通过前端设置页面配置：
# EMBY_URL, EMBY_API_KEY, EMBY_USER_ID
# TMDB_API_KEY, TMDB_LANGUAGE
# PROXY_ENABLED, PROXY_PROTOCOL, PROXY_HOST, PROXY_PORT
# SYNC_CRON
